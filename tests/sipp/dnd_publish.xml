<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  PATH B — DND state via inbound PUBLISH from a Cisco phone.

  Real Cisco Enterprise SIP firmware sends DND-state PUBLISHes with
  the phone's MAC address as the SIP user in every URI field
  (Request-URI, From, To, Contact, body entity), and the actual
  endpoint name only in the Authorization: Digest username field.
  Asterisk's stock res_pjsip_endpoint_identifier_user can't match
  the MAC against any endpoint, so res_pjsip_cisco_feature_events'
  cisco_authorization_identify is what claims the request — by
  matching the Authorization username against an endpoint that has
  a type=cisco section. That identification path is what makes
  our on_rx_request hook fire for PUBLISH; using the endpoint name
  in From instead short-circuits via stock identifiers and the
  request gets routed via pubsub_module to a 489 Bad Event.

  Wire-format reference: tcpdump of a CP-8865 (MAC 4cbc488b9e63)
  pressing DND on/off against the live PBX, captured 2026-05-19.
  This scenario uses a sentinel MAC `aabbccddee50` so failures
  aren't confused with real-phone traffic.

  Flow:
    1. PUBLISH with MAC-only URIs + presence body containing
       <ce:dnd/>; no Authorization.
    2. asterisk responds 401 — cisco_authorization_identify
       returns NULL (no Auth header), endpoint defaults to the
       artificial-endpoint, asterisk challenges.
    3. PUBLISH with Authorization carrying endpoint name 1050.
       cisco_authorization_identify matches by Authorization
       username, returns endpoint 1050. asterisk validates
       Digest. on_rx_request fires. Our hook claims the request,
       writes DND/1050 = on, sends 200 OK.
    4. Side-effect: pjsip cisco status 1050 reports DND/1050: on.
-->
<scenario name="cisco_dnd_publish">

  <!-- ============================================================
       Step 1: initial PUBLISH. MAC aabbccddee50 is a sentinel not
       used elsewhere.
       ============================================================ -->
  <send retrans="500">
    <![CDATA[

      PUBLISH sip:aabbccddee50@[remote_ip] SIP/2.0
      Via: SIP/2.0/TCP [local_ip]:[local_port];branch=[branch]
      From: <sip:aabbccddee50@[remote_ip]>;tag=[call_number]
      To: <sip:aabbccddee50@[remote_ip]>
      Call-ID: [call_id]
      CSeq: 1 PUBLISH
      Contact: <sip:aabbccddee50@[local_ip]:[local_port];transport=tcp>
      User-Agent: Cisco-CP8865/12.1.1
      Event: presence
      Expires: 3600
      Max-Forwards: 70
      Content-Type: application/pidf+xml
      Content-Length: [len]

      <?xml version="1.0" encoding="UTF-8"?>
      <presence xmlns="urn:ietf:params:xml:ns:pidf"
                xmlns:dm="urn:ietf:params:xml:ns:pidf:data-model"
                xmlns:e="urn:ietf:params:xml:ns:pidf:status:rpid"
                xmlns:ce="urn:cisco:params:xml:ns:pidf:rpid"
                entity="sip:aabbccddee50">
        <dm:person>
          <e:activities>
            <ce:dnd/>
          </e:activities>
        </dm:person>
      </presence>
    ]]>
  </send>

  <recv response="401" auth="true">
  </recv>

  <!-- ============================================================
       Step 3: PUBLISH with Authorization. SIPp's [authentication
       username=1050 password=ci-no-secret] computes the digest
       using endpoint 1050's credentials. cisco_authorization_identify
       matches the Authorization username field to endpoint 1050
       (which has a type=cisco section), and our on_rx_request hook
       fires for PATH B.
       ============================================================ -->
  <send retrans="500">
    <![CDATA[

      PUBLISH sip:aabbccddee50@[remote_ip] SIP/2.0
      Via: SIP/2.0/TCP [local_ip]:[local_port];branch=[branch]
      From: <sip:aabbccddee50@[remote_ip]>;tag=[call_number]
      To: <sip:aabbccddee50@[remote_ip]>
      Call-ID: [call_id]
      CSeq: 2 PUBLISH
      Contact: <sip:aabbccddee50@[local_ip]:[local_port];transport=tcp>
      User-Agent: Cisco-CP8865/12.1.1
      Event: presence
      Expires: 3600
      Max-Forwards: 70
      [authentication username=1050 password=ci-no-secret]
      Content-Type: application/pidf+xml
      Content-Length: [len]

      <?xml version="1.0" encoding="UTF-8"?>
      <presence xmlns="urn:ietf:params:xml:ns:pidf"
                xmlns:dm="urn:ietf:params:xml:ns:pidf:data-model"
                xmlns:e="urn:ietf:params:xml:ns:pidf:status:rpid"
                xmlns:ce="urn:cisco:params:xml:ns:pidf:rpid"
                entity="sip:aabbccddee50">
        <dm:person>
          <e:activities>
            <ce:dnd/>
          </e:activities>
        </dm:person>
      </presence>
    ]]>
  </send>

  <recv response="200">
  </recv>

  <ResponseTimeRepartition value="10, 20, 30, 50, 100, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000"/>

</scenario>
